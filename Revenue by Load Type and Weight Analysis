SELECT 
    method,
    AVG(weight_total) AS avg_weight,
    SUM(cust_total) AS total_revenue,
    SUM(gross_profit) AS total_profit
FROM 
    loads
GROUP BY 
    method
ORDER BY 
    total_revenue DESC, total_profit DESC;
